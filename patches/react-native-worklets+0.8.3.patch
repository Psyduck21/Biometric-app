diff --git a/node_modules/react-native-worklets/lib/module/memory/serializable.native.js b/node_modules/react-native-worklets/lib/module/memory/serializable.native.js
index 0baf8b9..f96a947 100644
--- a/node_modules/react-native-worklets/lib/module/memory/serializable.native.js
+++ b/node_modules/react-native-worklets/lib/module/memory/serializable.native.js
@@ -17,7 +17,10 @@ function isHostObject(value) {
   // the below workaround works well and is way faster than an additional JSI call.
   // We use the fact that host objects have broken implementation of `hasOwnProperty`
   // and hence return true for all `in` checks regardless of the key we ask for.
-  return MAGIC_KEY in value;
+  if (MAGIC_KEY in value) return true;
+  // Patch: Nitro Modules HybridObjects don't mock 'in', so we identify WorkletQueueFactory explicitly by reference
+  if (globalThis.__WORKLET_QUEUE_FACTORY && value === globalThis.__WORKLET_QUEUE_FACTORY) return true;
+  return false;
 }
 export function isSerializableRef(value) {
   'worklet';
diff --git a/node_modules/react-native-worklets/src/memory/serializable.native.ts b/node_modules/react-native-worklets/src/memory/serializable.native.ts
index a48d2fd..5d246d1 100644
--- a/node_modules/react-native-worklets/src/memory/serializable.native.ts
+++ b/node_modules/react-native-worklets/src/memory/serializable.native.ts
@@ -29,7 +29,10 @@ function isHostObject(value: NonNullable<object>) {
   // the below workaround works well and is way faster than an additional JSI call.
   // We use the fact that host objects have broken implementation of `hasOwnProperty`
   // and hence return true for all `in` checks regardless of the key we ask for.
-  return MAGIC_KEY in value;
+  if (MAGIC_KEY in value) return true;
+  // Patch: Nitro Modules HybridObjects don't mock 'in', so we identify WorkletQueueFactory explicitly by reference
+  if ((globalThis as any).__WORKLET_QUEUE_FACTORY && value === (globalThis as any).__WORKLET_QUEUE_FACTORY) return true;
+  return false;
 }
 
 export function isSerializableRef<TValue = unknown>(
